# Connection to database settings
spring.datasource.url = jdbc:oracle:thin:@ora3.elka.pw.edu.pl:1521:ora3inf
spring.datasource.username = msuchoc1
spring.datasource.password = msuchoc1

# Hibernate settings
spring.jpa.hibernate.ddl-auto = update
spring.jooq.sql-dialect = org.hibernate.dialect.Oracle10gDialect

# Logging settings
logging.level.root = INFO

# SLL settings
server.port = 8443
server.ssl.enabled = true
server.ssl.key-store = src/main/resources/samochodex.p12
server.ssl.key-store-password = verysecurepassword
server.ssl.key-store-type = PKCS12
server.ssl.key-alias = samochodex
